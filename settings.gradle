dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // GitHub
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "WetherApp"
include ':app'
